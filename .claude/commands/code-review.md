---
description: "코드 리뷰 수행"
---

# Code Review: 코드 리뷰

## 대상: $ARGUMENTS

## 목표

코드 변경 사항을 리뷰하여 품질, 보안, 패턴 준수를 확인합니다.

## 리뷰 프로세스

### 1. 변경 사항 파악

```bash
# 스테이징된 변경 확인
git diff --cached

# 또는 특정 커밋과 비교
git diff HEAD~1
```

### 2. 리뷰 체크리스트

#### 코드 품질
- [ ] 코드가 명확하고 이해하기 쉬운가?
- [ ] 불필요한 복잡성이 없는가?
- [ ] DRY 원칙을 따르는가?
- [ ] 적절한 에러 처리가 있는가?

#### 프로젝트 컨벤션
- [ ] CLAUDE.md 규칙을 준수하는가?
- [ ] BFF 패턴을 따르는가? (브라우저에서 DB 직접 호출 금지)
- [ ] React Query 사용 패턴이 올바른가?
- [ ] 파일 구조가 프로젝트 표준과 일치하는가?

#### 보안
- [ ] SQL 인젝션 가능성이 없는가?
- [ ] XSS 취약점이 없는가?
- [ ] 인증/인가가 올바르게 처리되는가?
- [ ] 민감한 정보가 노출되지 않는가?

#### 성능
- [ ] 불필요한 리렌더링이 없는가?
- [ ] N+1 쿼리 문제가 없는가?
- [ ] 적절한 인덱스를 사용하는가?

#### 테스트
- [ ] 충분한 테스트가 있는가?
- [ ] 엣지 케이스가 커버되는가?

### 3. 패턴 검증

**BFF 패턴 확인:**
```typescript
// ❌ 잘못된 예 - 브라우저에서 직접 DB 호출
const { data } = await supabase.from('vendors').select()

// ✅ 올바른 예 - API Route 통해 호출
import api from '@/api-client/client';

const { data } = useQuery({
  queryKey: ['vendors'],
  queryFn: async () => (await api.get('/api/vendors')).data.data
})
```

**React Query 패턴:**
```typescript
// ❌ 커스텀 훅으로 감싸지 않음
export const useVendors = () => useQuery({...})

// ✅ 컴포넌트에서 직접 사용
export function VendorList() {
  const { data } = useQuery({...})
}
```

## 리뷰 결과 템플릿

### 요약
- **전체 평가**: [Approve/Request Changes/Comment]
- **변경 파일 수**: X개
- **주요 변경 사항**:

### 긍정적인 점
-

### 개선 필요 사항

#### 🔴 필수 수정 (Must Fix)
- **파일**: `path/to/file.ts:line`
- **이슈**:
- **제안**:

#### 🟡 권장 수정 (Should Fix)
- **파일**: `path/to/file.ts:line`
- **이슈**:
- **제안**:

#### 🟢 선택 수정 (Nice to Have)
- **파일**: `path/to/file.ts:line`
- **제안**:

### 질문 사항
-

### 결론
